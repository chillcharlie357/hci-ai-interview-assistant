[project]
name = "hci-ai-interview-assistant"
version = "0.1.0"
description = "AI-assisted interview MVP with recruiter setup, LiveKit interview room, and auditable reports."
requires-python = ">=3.12"
dependencies = []

[tool.uv]
package = false
